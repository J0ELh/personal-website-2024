export interface ProjectFormat {
    projectName: string;
    affiliation: string;
    description: string;
  }
  

export interface RecommendationFormat {
    affiliation: string;
  }
  
export interface AwardFormat {
  affiliation: string;
  name: string;
}